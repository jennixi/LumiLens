enum Config {
    // ⚠️ Hackathon convenience. Don't commit real keys to public repos.
    
    static let geminiAPIKey = "<YOUR_GEMINI_API_KEY>"

    // Privacy flags
    static let allowCloudImageCaption = false  // set true ONLY if you want to upload images to Gemini
}