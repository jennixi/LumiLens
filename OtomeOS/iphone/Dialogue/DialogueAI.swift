import Foundation

enum DialogueAI {
    static func top3Options(fromTranscript t: String) -> [String] {
        // replace with real AI call later
        return [
            "Ask about the project",
            "Share your progress",
            "Suggest a study jam"
        ]
    }
}